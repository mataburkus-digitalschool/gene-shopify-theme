<section class="gene-sec gene-wrap" style="max-width:820px">
  <h1 style="font-family:var(--font-display);font-weight:800;font-size:clamp(28px,3.4vw,40px);letter-spacing:-.02em;margin-bottom:20px">{{ page.title }}</h1>
  <div style="font-size:16px;line-height:1.7;color:var(--fg-1)" class="rte">{{ page.content }}</div>
</section>