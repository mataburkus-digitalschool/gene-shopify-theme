<div class="g-wrap" style="padding:48px 24px;"><h1>Objednávka {{ order.name }}</h1>{% for line_item in order.line_items %}<p>{{ line_item.title }} × {{ line_item.quantity }} — {{ line_item.final_line_price | money }}</p>{% endfor %}<b>Celkem: {{ order.total_price | money }}</b></div>